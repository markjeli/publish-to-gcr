# name: publish

# on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: docker/setup-buildx-action@v1
    - uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.CR_PAT }}
    - uses: docker/build-push-action@v2
      with:
       context: .
       tags: ghcr.io/myapp:latest
       push: true

    # - name: Build the hello-world Docker image
    #   run: |
    #     docker build . --tag ghcr.io/markjeli/hello-docker-gcr-demo:latest
    #     docker run ghcr.io/markjeli/hello-docker-gcr-demo:latest
    #     docker push ghcr.io/markjeli/hello-docker-gcr-demo:latest
